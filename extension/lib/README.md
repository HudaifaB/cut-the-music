# Extension Runtime Assets

Place browser runtime files here.

The experimental AI path is prepared for a local ONNX Runtime Web bundle. Chrome extensions should load extension-packaged scripts instead of remote CDN scripts.

Expected future file examples:

```text
ort.min.js
ort-wasm-simd-threaded.wasm
ort-wasm.wasm
```
