# Extension Model Assets

Place browser-compatible model files here.

The experimental AI path currently looks for:

```text
models/voice-separator.onnx
```

It also expects ONNX Runtime Web to be bundled locally under:

```text
lib/
```

Do not commit large model files until the model license, size, and browser performance have been checked.
